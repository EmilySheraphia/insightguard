{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run InsightGuard API",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/application/app.py",
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}",
      "env": { "PYTHONPATH": "${workspaceFolder}" }
    }
  ]
}
